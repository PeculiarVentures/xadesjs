machine:
  node:
    version: 6.9.1

test:
  pre:
    - nmp run build:map
    - nmp run build:test

  override:
    - npm run coverage

  post:
    - npm run coveralls

general:
  branches:
    ignore:
      - gh-pages
